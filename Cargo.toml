[package]
name = "ctf2-writer"
version = "0.1.0"
edition = "2021"
description = "Native Rust writer for Common Trace Format 2 (CTF2)"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
uuid = { version = "1", features = ["v4"] }
